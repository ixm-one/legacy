include_guard(GLOBAL)

#[[ SYNOPSIS

This module holds functions and macros related to augmenting CMake. Most, if
not all of the other modules include this one.

]]

include(Base) # Just so things don't break right away
