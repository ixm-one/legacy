include_guard(GLOBAL)

# Placeholder file until this is implemented

# Override that lets us handle both signatures of tests, while also supporting
# building an executable if desired.
function(add_test)
endfunction()

# No-op so we can have full control over other projects while still generating
# our own test target.
function(enable_testing)
endfunction()
